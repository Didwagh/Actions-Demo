name: deployment
on: push

jobs:
  backend_server:
    runs-on: ubuntu-latest
    # outputs:
      # backend_url: ${{ steps.start-backend.outputs.url }}
    steps:
      - name: Checkout repo
        run: ssh -p 443 -R0:127.0.0.1:8080 qr@free.pinggy.io
