name: deployment
on: push

jobs:
  backend_server:
    runs-on: ubuntu-latest
    # outputs:
      # backend_url: ${{ steps.start-backend.outputs.url }}
    steps:
      - name: Checkout repo
        run: ./pinggy -p 443 -R0:localhost:8000
